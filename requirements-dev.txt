# ---------------------------------------------------------------------------
# Developer + CI tooling. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------

-r requirements.txt

# --- Testing ---
pytest==8.3.2
pytest-cov==5.0.0
chispa==0.10.1            # PySpark DataFrame assertions

# --- Linting / formatting / typing ---
ruff==0.6.4
black==24.8.0
mypy==1.11.2
types-requests==2.32.0.20240907
types-PyYAML==6.0.12.20240808

# --- Pre-commit ---
pre-commit==3.8.0

# --- Databricks tooling (CLI installed separately; SDK for local scripting) ---
databricks-sdk==0.30.0
